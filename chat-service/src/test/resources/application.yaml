spring:
  cloud:
    config:
      enabled: false
      import-check:
        enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true


eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false

jwt:
  secret: "thisIsADummySecretKeyForIntegrationTestingPurposes"
